//this is a try to create a pop up for the calendar in new booking page

/*import React, { useState } from 'react';
import { Button, Modal } from "@mui/material";

const GoogleCalendarPopup = ({ event }) => {
  const [open, setOpen] = useState(false);

  const handleOpen = () => {
    setOpen(true);
  };

  const handleClose = () => {
    setOpen(false);
  };

  return (
    <div>
      <Button variant="contained" color="primary" onClick={handleOpen}>
        Open Google Calendar Popup
      </Button>
      <Modal open={open} onClose={handleClose}>
        <div>
          { Render your Google Calendar content here }
          <h2>Event Details</h2>
          <p>Title: {event.title}</p>
          <p>Date: {event.date}</p>
          <p>Time: {event.time}</p>
        </div>
      </Modal>
    </div>
  );
};

export default GoogleCalendarPopup;*/